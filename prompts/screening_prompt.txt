You are a neurobiology research assistant. Screen the following paper for relevance to a candidate-gene discovery pipeline.

PAPER TITLE: {title}
PAPER TEXT (abstract or first pages excerpt):
{abstract}

FOCUS TOPIC: {topic}
FOCUS BEHAVIOR: {behavior}
TARGET ENTITY TYPE: {entity_type}

Classify this paper along three dimensions. Return a single JSON object with these fields:

- relevance: exactly one of
    * "core"       - paper presents ORIGINAL experiments directly testing how {entity_type} molecules regulate {behavior} during {topic} (e.g., RNAi/KO/Gal4 manipulation of a neuropeptide measuring starvation-induced locomotion)
    * "relevant"   - paper strongly informs the topic but matches only ONE side: {entity_type} biology without starvation-behavior testing, OR starvation-{behavior} without direct {entity_type} manipulation (e.g., AKH cell ablation measuring metabolism only; feeding assay without neuropeptide focus)
    * "peripheral" - indirectly related: REVIEW articles surveying the field, pure energy-metabolism studies, behavioral theory papers, comparative physiology. Useful as background but not a primary evidence source.
    * "unrelated"  - off-topic: social bonding, reproduction/oogenesis, immunity, non-insect mammalian physiology, or any topic with no meaningful connection to {topic} / {behavior} / {entity_type}.
- species: exactly one of "insect" | "mammal" | "other"
    * Use "mammal" if the experimental organism is rodent/mouse/rat/human (even if the paper mentions neuropeptides — these are cross-species contamination for an insect-focused pipeline).
- paper_type: exactly one of "primary" | "review"
    * "review" if the paper has no original experiments (surveys others' work, has no Methods/Results of its own).
- reason: ONE short sentence (max 200 chars) explaining the classification. Mention the specific molecule/assay/organism if it clarifies the call.
- confidence: your confidence 0.0-1.0

Rules:
- Base relevance on the paper's MAIN FOCUS, not incidental mentions. A paper that briefly cites NPF but studies oogenesis is "unrelated", not "relevant".
- For insect-focused pipelines, mammalian physiology papers (leptin, agouti-related peptide, POMC, etc.) are "unrelated" even if topically about feeding — they are cross-species contamination.
- Reviews are always "peripheral" or "relevant" at most (never "core"), regardless of how on-topic.
- If you cannot tell the paper type from the excerpt, default paper_type to "primary" and lower confidence.

CRITICAL — no example leakage: The example below shows JSON FORMAT only. Do NOT copy any value (molecule name, reason text, species) from the example into your answer. Every field value must reflect THIS paper's content.

Return a single JSON object (NOT an array). No text outside the object.

Example (FORMAT ONLY — do not copy these values):
{
  "relevance": "<core|relevant|peripheral|unrelated>",
  "species": "<insect|mammal|other>",
  "paper_type": "<primary|review>",
  "reason": "<one sentence justification specific to this paper>",
  "confidence": 0.9
}

Now classify the paper above. Return only the JSON object.
